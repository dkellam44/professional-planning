# Test environment for Coda MCP server
# Note: This is a test token - replace with real Coda API token for production

# Coda API Token (required)
CODA_API_TOKEN=test_coda_token_replace_me

# PostgreSQL configuration for token storage (optional for Phase 2)
# Commenting out for Phase 1 - using environment tokens
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=mcp_auth
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres_password

# Encryption key for token storage
MCP_AUTH_ENCRYPTION_KEY=test-encryption-key-32-bytes-long

# Bearer token for development/testing
BEARER_TOKEN=test_bearer_token_456
AUTH_MODE=both
LOG_LEVEL=debug