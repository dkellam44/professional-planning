services:
  coda-mcp:
    build:
      context: ../..
      dockerfile: services/coda-mcp.Dockerfile
    image: coda-mcp:latest
    env_file:
      - .env
    environment:
      - CODA_API_TOKEN=${CODA_API_TOKEN}
    ports:
      - "8080:8080"
