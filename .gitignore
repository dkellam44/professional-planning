# macOS
.DS_Store

# Node/venv if used later
node_modules/
venv/

# Editors
.vscode/
.idea/

# Archives & exports
archive/*.zip
*.xlsx

# Repository organization (Phase 0.5)
# Triage area - untracked, reviewed weekly
inbox/
y_collection_box/

# Archive directory - historical reference, not tracked
z_archive/

# Temp files
*.swp
*.tmp
*~

# MCP Secrets (never commit API keys or tokens)
.mcp/.env
.mcp/data/
/workspace/mcp-configs/user-scope/.env

# Infrastructure & Deployment Secrets (never commit .env files with credentials)
infra/config/.env
infra/config/.env.local
infra/config/.env.*
docs/ops/.env
docs/ops/.env.*
Portfolio/.env.*
