# Coda MCP Server Environment Variables
# Copy this file to .env and fill in your actual values

# Required: Coda API Token
# Get from: https://coda.io/account/settings/api
CODA_API_TOKEN=your_coda_api_token_here

# Optional: Bearer token for local development
# Use this when testing without Cloudflare Access
# BEARER_TOKEN=dev_token_123

# Optional: Authentication mode
# Options: cloudflare, bearer, both (default: both)
# AUTH_MODE=both

# Optional: Log level
# Options: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# Optional: Server configuration
# PORT=8080
# HOST=0.0.0.0

# Optional: Coda API base URL (usually don't change this)
# CODA_API_BASE_URL=https://coda.io/apis/v1

# Optional: Cloudflare Access configuration
# CLOUDFLARE_ACCESS_TEAM_DOMAIN=bestviable.cloudflareaccess.com
# CLOUDFLARE_ACCESS_AUD=bestviable

# Optional: PostgreSQL configuration for token storage
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=mcp_auth
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_postgres_password
# POSTGRES_MAX_CONNECTIONS=5

# Optional: Encryption key for token storage (required if using PostgreSQL)
# MCP_AUTH_ENCRYPTION_KEY=your-32-byte-encryption-key-here